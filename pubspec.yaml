name: stock_tracking_app
description: "Stock tracking with watchlists"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.18.0
  firebase_auth: ^4.0.0
  cloud_firestore: ^4.6.0
  http: ^1.2.2
  provider: ^6.0.3
  cupertino_icons: ^1.0.8
  intl: ^0.17.0
  syncfusion_flutter_core: ^20.4.54 # Update to the latest version
  syncfusion_flutter_charts: ^20.4.54
  url_launcher: ^6.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
